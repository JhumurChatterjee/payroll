<% provide(:title, @leave_type.leave_type) %>

<div class="row justify-content-center">
  <div class="col-sm-12 col-md-8 col-lg-7">
    <div class="card details-card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span>Leave Type - <%= @leave_type.leave_type %></span>
        <span>
          <%= link_to "Edit", edit_leave_type_path(@leave_type), class: "btn btn-sm btn-primary" %>
        </span>
      </div>

      <div class="card-body">
        <div class="row">
          <div class="col-md-6 col-sm-12 details-row">
            <label>Type</label>
            <span><%= @leave_type.leave_type %></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
